class WPM {
    constructor() {
        // Class for getting Windows Media information (the shit that shows up when you hit the volume keys)
        // And allowing that information to be gotten using function calls
        
        this.wpmLib = require("wpm");
    }
}